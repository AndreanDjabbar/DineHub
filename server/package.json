{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=development nodemon src/server.js",
    "migrate:dev": "dotenv -e .env.dev -- npx prisma migrate dev",
    "up-dev": "docker-compose --env-file .env.dev up -d",
    "reset:dev": "dotenv -e .env.dev -- npx prisma migrate reset --force",
    "generate:dev": "dotenv -e .env.dev -- npx prisma generate",
    "pull:dev": "dotenv -e .env.dev -- npx prisma db pull"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.16.2",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "dotenv": "^17.2.2",
    "drizzle-orm": "^0.44.7",
    "express": "^5.1.0",
    "ioredis": "^5.8.0",
    "joi": "^18.0.1",
    "jsonwebtoken": "^9.0.3",
    "midtrans-client": "^1.4.3",
    "morgan": "^1.10.1",
    "nodemailer": "^7.0.11",
    "postgres": "^3.4.7",
    "winston": "^3.17.0",
    "winston-daily-rotate-file": "^5.0.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "dotenv-cli": "^10.0.0",
    "prisma": "^6.16.2"
  }
}
